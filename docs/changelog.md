---
icon: lucide/scroll-text
---

# Changelog

This project follows semantic versioning. See the [GitHub releases](https://github.com/Polpetta/patrizio-bot/releases)
page for the latest release.

## Upcoming changes

- First version
- Add filters functionality
- Create project website
- Add `/prompt` command for AI-powered conversations via any OpenAI-compatible API,
  with reply-chain threading and chat ID allowlist
- Add per-chat AI memory: the model maintains a `memory.md` via tool calling
  (`read_memory`, `append_memory`, `update_memory`); users manage it with
  `/memory show|clear|enable|disable`; a 💾 reaction signals when the AI writes to memory

<!-- From here below we'll list all the older versions -->
