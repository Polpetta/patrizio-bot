[project]
name = "patrizio"
version = "0.1.0"
description = "A very simple Delta Chat bot designed for friends and meme groups"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [

]

[dependency-groups]
dev = [
    "zensical==0.0.27",
]
